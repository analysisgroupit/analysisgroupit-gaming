# ANALYSISGROUPIT Gaming

GitHub Pages ready — no npm, no build step.

Upload every file from this ZIP directly to the repository root.

Then:
Settings → Pages → Source: Deploy from a branch → main → /(root) → Save

The hero image is intentionally cropped to the gaming setup only, so the HTML headline does not duplicate text.

Product names and prices are demo catalog values until replaced with the final commercial catalog.
